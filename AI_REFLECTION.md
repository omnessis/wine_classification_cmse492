Final AI reflection: I used GitHub's Copilot Agent to aid me and building this repository.  I asked the agent to create the repo structure, create a logistic regression model with a notebook workflow, and to help me debug any issues that arose. At first when the agent created the repository, it created a src file, a test file, a README.md, and a data file.  The first thing I checked was the data file, I did not upload the data, I only gave the agent the data specs.  It had found the data on Kaggle and uploaded it to the repository without me asking.  After verifying it was the correct data, I chose to leave it alone.  After that, I asked the agent to fix the workflow and convert the current files into a notebook.  This took some back and fourth with the agent.  After a lot of breaks and fixes, when I went to push the working notebook to my repo, it showed up as a corrupt JSON file.  I then had to ask the agent to fix this issue.  Eventually, I was able to push a working notebook with a logistic regression model to my repo.  I changed quite a few things within my repo, I changed some information in the README, I changed the .gitignore to fit my preferences, and I changed any structure issues I had.

Overall, this experience was frustrating.  I spent a fair amount of time going back and fourth with an AI agent.  A lot of the time, the agent would fix one issue but then break something else.  It felt like the AI was too involved, it could see the whole enitre repository each time I asked it to change something.  It would edit multiple files at once to fit the changes it made but it wasn't always for the best.  It also made me uneasy to have the agent running commands through my terminal.  I read through what it was doing every time but it made me nervous that it could disrupt other files on my computer.  This was an experience and I would use AI again to help me with future projects.  However, I would perfer to use an AI agent that is a little more removed so I can have better control over what it is doing.